services:
  health-check:
    image: jeanmonse16/health-check
    build:
      context: .
      dockerfile: Dockerfile-healthcheck
    restart: "always"
    depends_on:
      auth:
        condition: service_started
  auth:
    image: jeanmonse16/auth
    build:
      context: .
      dockerfile: Dockerfile-auth
    restart: "always"
    ports:
      - "50051:50051"